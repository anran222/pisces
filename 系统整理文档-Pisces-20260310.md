Pisces 系统整理文档

## 文档定位

这份文档用于整理当前仓库中的系统现状，目标是帮助在“整理阶段”快速理解系统边界、模块职责、核心链路、依赖关系和当前限制。

这不是实施方案，不包含发布计划、灰度方案、DDL 设计模板或实施清单。

## 系统概览

Pisces 是一个面向 A/B 测试场景的实验系统，核心定位是提供一套可以独立运行的实验基础设施。系统当前以“无用户系统版本”为主，业务方不需要接入统一登录态，而是使用 `visitorId` 作为实验参与者标识。

当前系统已经覆盖以下能力：

- 实验创建、更新、启动、暂停、恢复、停止、删除。
- 基于 `visitorId` 的流量分组。
- VIEW、CLICK、CONVERT 三类事件上报。
- 实验统计、显著性检验、贝叶斯分析、SRM、序贯检验等分析能力。
- AI 解读、AI 实验设计建议。
- 文本/图像变体生成与实验演示数据生成。

当前系统不包含以下能力：

- 管理后台 UI。
- 完整权限体系与多租户隔离。
- 关系型数据库持久化。
- 消息队列和异步任务体系。
- 生产级监控、告警、审计与发布治理。

## 技术栈与运行形态

当前工程是一个多模块 Maven 项目：

- Java 21
- Spring Boot 3.2.0
- Maven
- Redis
- Zookeeper
- DashScope / 通义千问

运行参数来自 [`application.yml`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/resources/application.yml)：

- 服务端口：`9990`
- Context Path：`/api`
- 默认 Profile：`dev`
- Redis：`localhost:6379`
- Zookeeper：`localhost:2181`
- 通义模型：`qwen-plus`

## 工程结构

### `pisces-common`

公共模型与 DTO 模块，主要包含：

- 实验模型：[`Experiment.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/Experiment.java)
- 实验组模型：[`ExperimentGroup.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/ExperimentGroup.java)
- 流量配置模型：[`TrafficConfig.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/TrafficConfig.java)
- 事件模型：[`Event.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/Event.java)
- 统计模型：[`Statistics.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/Statistics.java)
- 实验元数据：[`ExperimentMetadata.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/ExperimentMetadata.java)
- 分层模型：[`ExperimentLayer.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/ExperimentLayer.java)

### `pisces-service`

业务逻辑模块，负责系统的核心能力：

- 实验管理：[`ExperimentServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/ExperimentServiceImpl.java)
- 流量分组：[`TrafficServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/TrafficServiceImpl.java)
- 事件采集：[`DataServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/DataServiceImpl.java)
- 统计分析：[`AnalysisServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/AnalysisServiceImpl.java)
- 多臂老虎机：[`MultiArmedBanditServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/MultiArmedBanditServiceImpl.java)
- 配置管理：[`ConfigServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/ConfigServiceImpl.java)
- 变体生成：[`VariantGenerationServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/VariantGenerationServiceImpl.java)

### `pisces-api`

对外 API 模块，负责控制器、鉴权拦截和日志处理：

- 实验接口：[`ExperimentController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/experiment/ExperimentController.java)
- 流量接口：[`TrafficController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/traffic/TrafficController.java)
- 数据接口：[`DataController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/data/DataController.java)
- 分析接口：[`AnalysisController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/analysis/AnalysisController.java)
- 变体接口：[`VariantController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/variant/VariantController.java)
- 数据生成接口：[`ExperimentDataGeneratorController.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/experiment/ExperimentDataGeneratorController.java)

## 核心对象

### 实验对象

实验对象记录实验 ID、名称、描述、状态、开始时间、结束时间、创建时间等基础信息。状态枚举包括：

- `DRAFT`
- `RUNNING`
- `PAUSED`
- `STOPPED`

### 实验元数据

实验元数据是系统里的核心配置载体，包含：

- 实验基础信息。
- 实验组列表。
- 流量配置。
- 白名单和黑名单。
- `configVersion`。
- `layerId`。

### 事件对象

事件对象记录一次实验行为，主要字段包括：

- `experimentId`
- `userId`
- `groupId`
- `eventType`
- `eventName`
- `properties`
- `timestamp`

这里有一个兼容性细节：代码里字段名仍然叫 `userId`，但当前语义实际是 `visitorId`。

## 核心能力整理

### 1. 实验管理

实验管理能力由 [`ExperimentServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/ExperimentServiceImpl.java) 实现，特点如下：

- 创建实验时自动生成实验 ID。
- 创建后默认状态为 `DRAFT`。
- 启动实验只允许从 `DRAFT` 进入 `RUNNING`。
- 暂停实验只允许从 `RUNNING` 进入 `PAUSED`。
- 恢复实验只允许从 `PAUSED` 回到 `RUNNING`。
- 删除实验时同时删除配置。

实验配置最终会通过 `ConfigService` 保存。

### 2. 流量分组

流量分组能力由 [`TrafficServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/TrafficServiceImpl.java) 实现，执行顺序大致如下：

1. 读取实验配置。
2. 尝试命中 Redis 分组缓存。
3. 校验实验状态是否为 `RUNNING`。
4. 校验白名单、黑名单和时间窗口。
5. 校验是否落在实验总流量范围内。
6. 按策略分配实验组。
7. 写入 Redis 缓存。

支持的分组策略包括：

- `RANDOM`
- `HASH`
- `RULE`
- `THOMPSON_SAMPLING`
- `UCB`

其中：

- `HASH` 使用 `visitorId + experimentId` 做一致性哈希。
- `THOMPSON_SAMPLING` 和 `UCB` 都依赖 Redis 中维护的学习状态。
- 当前 `RULE` 没有单独实现，实际回退到哈希分配。

### 3. 分层能力

系统已经定义了实验分层模型 [`ExperimentLayer.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/model/ExperimentLayer.java)，支持两种模式：

- `MUTEX`
- `ORTHOGONAL`

当前 `TrafficServiceImpl` 已经支持基于 `layerId` 的互斥检查：如果访客已经命中同层的其他实验，则当前实验拒绝分配。

### 4. 事件采集

事件采集能力由 [`DataServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/DataServiceImpl.java) 实现。

当前支持事件类型：

- `VIEW`
- `CLICK`
- `CONVERT`

上报处理逻辑：

- 若 `properties.clientEventId` 存在，则先做去重。
- 通过流量服务获取访客所属实验组。
- 写入事件明细。
- 更新事件计数器。
- 更新实验组访客集合。
- 若为 `CONVERT`，同步更新 MAB 奖励。

### 5. 分析能力

分析能力集中在 [`AnalysisServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/AnalysisServiceImpl.java)。

当前已实现的分析能力包括：

- 实验统计汇总。
- 实验组对比。
- 传统显著性检验。
- 样本量估算。
- 贝叶斯分析。
- 提前终止判断。
- 因果推断。
- HTE 分析。
- 敏感群体识别。
- 实验报告导出。
- 时间线分析。
- AI 智能解读。
- AI 实验设计。
- 自动毕业决策。
- 实验完成时间预测。
- SRM 检测。
- 序贯检验。

需要注意的现状：

- 时间线接口当前返回的是模拟数据，不是真实时间聚合结果。
- AI 分析在外部模型不可用时会退化为规则分析或模拟文案。

### 6. MAB 学习能力

多臂老虎机能力由 [`MultiArmedBanditServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/MultiArmedBanditServiceImpl.java) 实现。

当前实现包括：

- Thompson Sampling
- UCB

数据保存在 Redis 中：

- Beta 参数用于 Thompson Sampling。
- `trials / successes / averageReward` 用于 UCB。
- `CONVERT` 事件默认被视为一次正向奖励。

### 7. AI 变体生成

变体生成能力由 [`VariantGenerationServiceImpl.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/service/impl/VariantGenerationServiceImpl.java) 实现。

当前能力包括：

- 文本变体生成。
- 图像生成。
- 图生图。
- 图片局部编辑。
- 图片风格转换。
- 变体筛选。
- 变体评估。

当前 AI 能力有明确降级路径：

- 如果 `tongyi.enabled=false`，直接返回模拟数据。
- 如果 API Key 未配置，直接返回模拟数据。
- 如果调用 DashScope 失败，回退到模拟数据。

## 存储与配置整理

### Redis 的角色

Redis 是当前系统运行期的核心依赖，主要承担：

- 访客分组缓存。
- 事件明细存储。
- 事件计数存储。
- 实验组访客去重集合。
- MAB 学习状态存储。

主要 Key 前缀如下：

- `pisces:traffic:group:`
- `pisces:layer:assign:`
- `pisces:event:store:`
- `pisces:event:counter:`
- `pisces:visitor:set:`
- `pisces:event:dedup:`
- `pisces:mab:beta:`
- `pisces:mab:ucb:`
- `pisces:mab:trials:`

### Zookeeper 的角色

Zookeeper 主要承担实验配置中心职责：

- 保存实验配置。
- 保存分层配置。
- 监听配置变更。
- 更新本地缓存。

当前实现还支持一个简化回退：

- 当 Zookeeper 不可用时，配置会落到进程内缓存。
- 这个回退只对当前实例有效，实例重启后不会保留。

## 对外接口整理

### 实验管理接口

- `POST /api/experiments`
- `PUT /api/experiments/{id}`
- `GET /api/experiments/{id}`
- `GET /api/experiments`
- `POST /api/experiments/{id}/start`
- `POST /api/experiments/{id}/pause`
- `POST /api/experiments/{id}/resume`
- `POST /api/experiments/{id}/stop`
- `DELETE /api/experiments/{id}`

### 流量接口

- `POST /api/traffic/assign`
- `GET /api/traffic/experiment/{experimentId}/mab/beta`
- `GET /api/traffic/experiment/{experimentId}/mab/stats`
- `GET /api/traffic/experiment/{experimentId}/mab/probabilities`
- `GET /api/traffic/experiment/{experimentId}/mab/summary`

### 数据接口

- `POST /api/data/event`

### 分析接口

- `GET /api/analysis/experiment/{id}/statistics`
- `GET /api/analysis/experiment/{id}/compare`
- `GET /api/analysis/experiment/{id}/significance`
- `GET /api/analysis/sample-size`
- `GET /api/analysis/experiment/{id}/bayesian`
- `GET /api/analysis/experiment/{id}/early-stop`
- `POST /api/analysis/experiment/{id}/causal-inference`
- `POST /api/analysis/experiment/{id}/hte`
- `POST /api/analysis/experiment/{id}/sensitive-groups`
- `GET /api/analysis/experiment/{id}/report`
- `GET /api/analysis/experiment/{id}/timeline`
- `GET /api/analysis/experiment/{id}/ai-insights`
- `POST /api/analysis/experiment/ai-design`
- `GET /api/analysis/experiment/{id}/auto-graduate`
- `GET /api/analysis/experiment/{id}/predict-completion`
- `GET /api/analysis/experiment/{id}/srm`
- `GET /api/analysis/experiment/{id}/sequential`

### 变体接口

- `POST /api/variants/text/generate`
- `POST /api/variants/image/generate`
- `POST /api/variants/image/generate-from-image`
- `POST /api/variants/image/edit`
- `POST /api/variants/image/style-transfer`
- `GET /api/variants/image/styles`
- `POST /api/variants/filter`
- `POST /api/variants/evaluate`

## 鉴权与异常处理

### 鉴权

系统已经实现了 API Key 拦截器：

- 拦截器实现见 [`ApiKeyAuthInterceptor.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/security/ApiKeyAuthInterceptor.java)
- 注册入口见 [`WebMvcConfig.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-api/src/main/java/com/pisces/api/security/WebMvcConfig.java)

当前实际行为是：

- 命中 `skip-paths` 的请求直接放行。
- 标注 `@NoTokenRequired` 的接口直接放行。
- 其余接口需要 `X-Pisces-Api-Key`。

但当前大多数业务接口已经标注 `@NoTokenRequired`，所以整体上仍偏开放。

### 异常处理

统一异常处理见 [`GlobalExceptionHandler.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-service/src/main/java/com/pisces/service/exception/GlobalExceptionHandler.java)，主要覆盖：

- 参数校验异常。
- 业务异常。
- 通用系统异常。

响应码定义见 [`ResponseCode.java`](/Users/a147735/IdeaProjects/personal/pisces/pisces-common/src/main/java/com/pisces/common/enums/ResponseCode.java)。

## 当前系统的几个关键信号

### 1. 当前系统更像“可运行的实验平台原型”

它已经具备完整链路，但还没有进入严格意义上的生产化治理阶段。

### 2. Redis 是核心运行依赖

虽然仓库文档里提到 Redis 可选，但从当前代码实现看，流量分组、事件采集、统计分析都依赖 Redis。

### 3. Zookeeper 主要负责配置，不负责长期持久化兜底

Zookeeper 不可用时只有本地缓存兜底，重启后配置不会自动恢复。

### 4. AI 能力是“可用但可降级”的

系统已经接好了 DashScope，但也明确保留了 mock / fallback 路径，因此当前整理阶段更适合把 AI 能力理解成“增强项”，而不是系统主干依赖。

### 5. 文档与代码存在口径差异

部分 README 描述偏产品宣传或理想状态，而当前代码更适合被定义为“具备实验全链路的服务端原型系统”。后续整理时需要把“已实现能力”和“规划能力”分开。

## 当前待继续整理的问题

1. `configVersion` 已存在，但在实验更新流程中没有看到明确的自增逻辑，需要确认缓存失效机制是否完整。

2. 时间线接口当前是模拟实现，需要确认是否保留为演示接口，还是改造成真实统计接口。

3. `RULE` 流量策略目前没有独立实现，需要明确是占位能力还是短期要补齐的功能。

4. AI 分析、AI 设计、图片生成等能力依赖外部服务，需要进一步梳理调用链、限流、成本和超时策略。

5. 仓库中的 README、完整指南、API 说明、实际代码实现之间存在一定偏差，后续需要统一口径。

6. 当前缺少一份专门面向整理阶段的“系统现状图”和“能力边界清单”，后续可以单独补一版更短的总览文档。
